import React from 'react';

/**
 * DemoModeSelector - DISABLED
 * This component has been disabled per user request.
 * The Info/Home/Demo toggle is hidden from all users.
 */
export const DemoModeSelector = () => {
  // Component completely disabled - hidden from all users including super admin
  return null;
};
