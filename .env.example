# Example environment variables for local development

# Supabase Configuration
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stream Chat
VITE_STREAM_API_KEY=
STREAM_API_SECRET=

# Google Maps (Required for Places tab to work)
# Get from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_MAPS_API_KEY=

# OAuth Provider Configuration
# Set to 'true' to show OAuth buttons in the auth modal.
# Only enable after configuring the provider in Supabase Dashboard:
# - Google: Authentication > Providers > Google (free to setup)
# - Apple: Authentication > Providers > Apple (requires Apple Developer account)
VITE_OAUTH_GOOGLE_ENABLED=false
VITE_OAUTH_APPLE_ENABLED=false
